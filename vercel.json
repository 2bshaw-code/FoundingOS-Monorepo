{
  "installCommand": "npm install --include=dev",
  "buildCommand": "cd core-operations/backend && npx prisma generate && npx prisma migrate deploy && cd ../.. && node scripts/build-core-operations-api.mjs",
  "functions": {
    "api/index.js": {
      "runtime": "nodejs24.x",
      "includeFiles": "api/**"
    }
  },
  "rewrites": [
    { "source": "/(.*)", "destination": "/api" }
  ]
}
